<Page
    x:Class="AlienCompadre.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:AlienCompadre"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="using:AlienCompadre.ViewModel"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Page.DataContext>
        <vm:ClsMainPageVM></vm:ClsMainPageVM>
    </Page.DataContext>
    
    <StackPanel>
        <Grid Grid.Row="1" Grid.ColumnSpan="2" Background="Bisque">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Image x:Name="playerImage" Grid.Column="0" Margin="10" Source="{Binding Player.SrcImage, Mode=OneWay}" Width="50" Height="50"></Image>
            <StackPanel Grid.Column="0" Orientation="Horizontal" Height="Auto">
                <TextBlock Text="Ammo: " Margin="10" FontSize="20"/>
                <TextBlock Name="ammo" FontSize="20" Margin="0, 10, 10, 10" Text="{Binding Player.Ammo}"/>
            </StackPanel>

            <StackPanel Grid.Column="1" Orientation="Horizontal" Height="Auto">
                <TextBlock Text="Dungeons completed: " Margin="10" FontSize="20"/>
                <TextBlock Name="dungeonsCompleted" FontSize="20" Margin="0, 10, 10, 10" Text="{Binding CompletedDungeons}"/>
            </StackPanel>
            
        </Grid>
        <GridView x:Name="gridView01" ItemsSource="{Binding Mazmorra.Tablero}" HorizontalAlignment="Center" VerticalAlignment="Center">
            <GridView.ItemTemplate>
                <DataTemplate>
                    <Image Source="{Binding Image, Mode=TwoWay}" Width="100" Height="100">
                    </Image>
                </DataTemplate>
            </GridView.ItemTemplate>
            <GridView.ItemsPanel>
                <ItemsPanelTemplate>
                    <ItemsWrapGrid MaximumRowsOrColumns="8" Orientation="Horizontal"></ItemsWrapGrid>
                </ItemsPanelTemplate>
            </GridView.ItemsPanel>
        </GridView>
    </StackPanel>
</Page>
